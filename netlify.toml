[build.environment]
  NODE_VERSION = "18"

[build]
  command = "npm run build"
  publish = "out"

# Wyłączenie przestarzałego pluginu - używamy static export
[[plugins]]
  package = "@netlify/plugin-nextjs"
  [plugins.inputs]
  disabled = true