[build.environment]
  NODE_VERSION = "18"
  NETLIFY_NEXT_PLUGIN = "false"
  NETLIFY_ENABLE_FRAMEWORK_DETECTION = "false"

[build]
  command = "npm run build"
  publish = "out"

# Całkowicie bez pluginów Next.js - używamy tylko static export